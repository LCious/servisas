<Window x:Class="servisas.PasswordInput"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter Passcode" Height="150" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Enter passcode to unlock this bike:" Margin="10"/>
        <PasswordBox x:Name="PasswordBox" Margin="10" Grid.Row="1"/>
        <Button Content="OK" Click="OKButton_Click" Width="75" Margin="10" Grid.Row="2"/>
    </Grid>
</Window>
